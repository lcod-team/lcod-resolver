schemaVersion = "1.0"
id = "lcod://resolver/internal/prepare-cache@1"
name = "prepare-cache"
namespace = "resolver/internal"
version = "0.1.0"
kind = "function"
summary = "Compute the cache root directory for the resolver execution."

[tool]
name = "prepare_cache"
description = "Select cache root based on project path and cache-dir axiom."
inputSchema = "schema/input.json"
outputSchema = "schema/output.json"

[deps]
requires = [
  "lcod://tooling/resolver/cache-dir@1",
  "lcod://axiom/path/join@1",
  "lcod://tooling/script@1"
]

[docs]
readme = "README.md"
