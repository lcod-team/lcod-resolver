schemaVersion = "1.0"
id = "lcod://tooling/resolver/internal/summarize-result@0.1.0"
name = "summarize-result"
namespace = "tooling/resolver/internal"
version = "0.1.0"
kind = "function"
summary = "Extract dependency graph, integrity and warnings from resolver output."

[tool]
name = "summarize_result"
description = "Summarise resolverResult into dependency graph data."
inputSchema = "schema/input.json"
outputSchema = "schema/output.json"

[deps]
requires = [
  "lcod://impl/set@1",
  "lcod://flow/if@1",
  "lcod://tooling/value/is_array@0.1.0",
  "lcod://tooling/value/is_string_nonempty@0.1.0"
]

[docs]
readme = "README.md"
